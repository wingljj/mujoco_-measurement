# 恢复 MuJoCo 模型图设计

## 目标

在当前“经纬仪测站转移与到位稳定后测量”论文中恢复早期 MuJoCo 模型图，同时完整保留现有 8 幅统一风格的 TikZ/pgfplots 图。恢复内容包括 MuJoCo 运输过程快照和三维运输轨迹，不删除或替换现有数据图。

## 图片与位置

1. 将 `outputs/paper_figures/rendered_transport_snapshots.pdf` 插入第 3 节“系统与刚性负载代理模型”，用于展示 KUKA KR20、末端刚性负载代理和 MuJoCo 仿真场景。
2. 将 `outputs/figures/trajectory_3d_render.pdf` 插入第 7 节“轨迹生成与代表性任务”，用于展示原始 MuJoCo 任务中的三维末端运输轨迹。
3. 两幅恢复图按 15 cm 通栏图排版，优先使用矢量 PDF；Word 资产继续使用其对应 PNG。

## 科学表述边界

恢复图统一解释为历史 MuJoCo 模型和仿真轨迹的可视化证据。图题及相邻正文必须说明：末端几何是经纬仪的简化刚性负载代理，只用于显示末端竖轴姿态与运输过程，不代表真实经纬仪的结构、质量、惯量、夹持柔顺性或测量标定模型。

运输阶段仍不执行测量。论文继续保持“机械臂到达目标位姿并稳定后才进行经纬仪测量”的任务边界，不把运输倾角直接换算为测量误差。

## 编号与引用

论文图片总数由 8 幅增加至 10 幅。按正文首次出现顺序统一自动编号，并同步修订所有手工图号引用：

1. 规划、执行与稳定后测量流程；
2. MuJoCo 机械臂持经纬仪刚性代理的运输过程快照；
3. 五区域工作空间分布；
4. 原始 MuJoCo 任务的三维末端运输轨迹；
5. 五阶段运输姿态示意；
6. 代表性任务综合结果；
7. 四种残差配置比较；
8. 位置误差、倾角和姿态裕度；
9. 动态扰动代理指标；
10. 联合残差与优先级 IK 基线比较。

正文使用自动交叉引用或与上述顺序一致的明确图号，避免图题和正文出现重复、错号或旧编号残留。

## 构建与验证

修改 Markdown 正文和 PDF 构建过滤器，使两类既有 PDF 均以矢量形式嵌入。重新生成完整论文 PDF，并执行以下检查：

- PDF 可成功解析，所有字体嵌入；
- 10 幅图片均出现且图题没有重复编号；
- 两幅恢复图清晰、无裁切、无拉伸；
- 所有正文图号引用与实际编号一致；
- 逐页渲染检查分页、留白、表格和图片位置；
- 严格数据与主题完整性检查继续通过。

## 范围限制

本次不修改 MuJoCo XML、实验数据或图内原始仿真内容，不建立新的经纬仪三维资产，也不据恢复图新增真实仪器性能主张。
